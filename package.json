{
  "name": "violyra",
  "version": "1.5.0",
  "type": "module",
  "description": "Video making agent skills for music video production",
  "main": ".claude-plugin/plugin.json",
  "scripts": {
    "test": "vitest run",
    "lint-skills": "node scripts/lint-skills.js",
    "dev:app": "pnpm exec dotenv -- pnpm --filter @vima/app dev",
    "build:app": "pnpm exec dotenv -- pnpm --filter @vima/app build",
    "start:app": "pnpm exec dotenv -- pnpm --filter @vima/app start"
  },
  "keywords": ["skills", "video", "music-video", "replicate", "ffmpeg", "seedance", "lyrics"],
  "author": "soasme",
  "license": "MIT",
  "packageManager": "pnpm@10.30.2",
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "vitest": "^3.2.4"
  }
}
